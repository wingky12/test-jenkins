version: '3.7'

services:
  app:
    image: wingky/php8.1
    container_name: app
    hostname: app
    restart: unless-stopped
    ports:
      - "8090:80"
    networks:
      - internal


networks:
  internal:
